#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

double rat,a[1005];
int n,l,r,sum;

bool check(int x)
{
	double 
}

int main()
{
	scanf("%d",&n);
	for(int i=0;i<n;i++)
		scanf("%lf",&a[i]),sum+=round(a[i]);
	for(int i=0;i<=500;i++)
	{
		if(check(i)) 
		{
			printf("%d",i);
			return 0;
		}
	}
	return 0;
}
