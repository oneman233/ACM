#include <bits/stdc++.h>
using namespace std;

const int maxn=300000;

int n;
long long a[maxn+5],b[maxn+5];

int main()
{
    scanf("%d",&n);
    for(int i=1;i<=n;++i)
        scanf("%lld",&a[i]);

    return 0;
}
