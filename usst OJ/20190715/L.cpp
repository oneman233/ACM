#include <bits/stdc++.h>
#define int long long
#define scl(a) scanf("%lld",&a)
#define prl(a) printf("%lld",a)
using namespace std;
typedef long long ll;

signed main()
{
    int r,h,y;
    scl(r);
    scl(h);
    scl(y);
    cout<<__gcd(r,h);
    return 0;
}
